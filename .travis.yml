language: python
python:
  - "2.7"
  - "3.6"

install:
  - pip install .
  - pip install -r requirements.pip

services:
  - postgresql

before_script:
  - psql -c 'create database test_db;' -U postgres

script: python django_imager/manage.py py.test